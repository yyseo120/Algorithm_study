def solution(arr, divisor):
    answer = []
    for i in arr:
        if (i%divisor == 0):
            print(i/divisor)
            answer.append(i)
            answer = sorted(answer)
    if answer == []:
        return [-1]
            
        
    return answer
    
