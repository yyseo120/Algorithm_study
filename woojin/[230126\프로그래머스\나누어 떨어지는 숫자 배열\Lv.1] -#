def solution(arr, divisor):
    l = [i for i in arr if i % divisor == 0]
    return sorted(l) if len(l) else [-1]
    
"""
나누어 떨어지는 수를 리스트 컴프리헨션 방식으로 l에 저장했다.
이후 len(l)의 boolean이 True인 경우(=길이가 0이 아닌 경우) 정렬된 l을 리턴하고,
길이가 0인 경우에는 [-1]을 반환하도록 했다.
"""
