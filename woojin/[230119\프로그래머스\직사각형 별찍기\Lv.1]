########## My Solution(1) ##########

a, b = map(int, input().strip().split(' '))
for i in range(b):
    for j in range(a):
        print('*', end='') #가로로 a번 별 찍기
    print() #줄 바꾸기(= \n )

"""
- 가로로 a번 별을 찍은 뒤 엔터
- 이걸 b번 하면 세로로 b줄이 됨

하지만 파이썬은 문자열을 다루는 강력한 방식을 지원하고 있으므로
보다 간단한 방법으로 만들 수 있음
"""

####################################

########## My Solution(2) ##########

a, b = map(int, input().strip().split(' '))
print((('*' * a) + '\n') * b)

"""
'별 a번 찍고 엔터'를 b번 반복
"""

####################################
