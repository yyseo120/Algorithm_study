def solution(n):
    return int("".join(sorted(str(n),reverse=True)))

"""
n을 문자열로 변환( = str(n) )한 뒤 내림차순으로 정렬( = sorted(~~) )했다.
이렇게 하면 문자열 한 개씩 분리된 리스트가 만들어지는데
이를 합치기 위해 "".join(~~~) 를 해주었다.

지금까진 문자열을 다룬 것인 만큼 int로 변환하여 최종 반환했다.
"""
