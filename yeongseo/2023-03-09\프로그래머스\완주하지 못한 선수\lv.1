# 정렬후 순서 비교
def solution(participant, completion):
    l=[]
    a=sorted(participant)
    b=sorted(completion)
    for i in range(len(b)):
        if a[i] == b[i]:
            pass
        else:
            return a[i]
    return a[-1]
