def solution(arr , divisor):
    return(sorted([i for i in arr if i % divisor == 0])) or [-1]
==================================================================================    
리스트컴프리핸션으로 원소가 나누어 떨어질 경우 리스트를 오름차순으로 정렬해주어 반환하고 
나머지가 0이 아니어서 빈리스트가 반환되면 앞이 False라 뒤까지 확인하여 반환한다.
<단락평가>에 대한 설명 참조
https://marinelifeirony.tistory.com/83
