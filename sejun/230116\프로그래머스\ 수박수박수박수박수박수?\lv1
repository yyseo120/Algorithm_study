def solution(n):
    lst = []
    a = "수"
    b = "박"
    for index in range(1,10000): 
        if index % 2 != 0:
            lst.append(a)
        else:
            lst.append(b)
    return(''.join(lst[:n]))
