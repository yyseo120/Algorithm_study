#자연수 n을 뒤집어 각 자리 숫자를 원소로 가지는 배열 형태로 리턴하기
def solution(n):
    lst = list(str(n))[-1::-1] #자연수 n을 문자열타입으로 변경후 리스트로 만들어줌. 배열은 슬라이싱으로 -1자리부터 반대로 첫자리까지 해서 변수를 씌워줌.
    return(list(map(int, lst))) #map함수를 이용해 원소를 string타입에서 int타입으로 변경해주고 리스트로 만들어 반환해줌. 
