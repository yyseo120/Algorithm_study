def solution(n):
    x = 0
    k = int(n**(1/2))
    for i in range(1, k + 1):
        if n**(1/2) == i:
            x = i
        else:
            continue
    answer = -1 if x == 0 else (x+1)**2
    return answer
