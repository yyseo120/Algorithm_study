def solution(n):
    answer = 0
    i = n ** 0.5
    
    if i == int(i):
        answer = (i+1)**2
    else:
        answer = -1
    return answer

# 양의 정수 n에 대해, 어떤 양의 정수 x의 제곱인지 아닌지 판단.
# n이 양의 정수 x의 제곱이라면 x+1의 제곱을 리턴
# 아니라면 -1을 리턴하는 함수
