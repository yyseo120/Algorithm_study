def solution(a, b):
    answer=[]
    for m in range(len(b)):
        i,j,k = b[m] 
        sor = sorted(a[i-1:j])
        answer.append(sor[k-1])
    return answer

```
배열의 i번째 숫자부터 j번째 숫자까지 자르고 정렬했을 떄, k 번째에 있는 수를 구한다.
