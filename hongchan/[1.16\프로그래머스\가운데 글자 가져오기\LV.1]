def solution(s):
    answer = ''
    mid = len(s)//2
    
    for i in range(len(s)):
        if len(s)%2 == 0:
            answer = s[mid-1]+s[mid]
        else:
            answer = s[mid]
            
        
    return answer
