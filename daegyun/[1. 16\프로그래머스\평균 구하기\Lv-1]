def solution(arr):
    answer = 0

    for i in range(len(arr)):
        answer += i / len(arr)
        
    return answer

solution([1, 2, 3, 4])


# 엄청난 1줄코드.. ㄷㄷㄷ
def solution(arr):
    answer = sum(arr) / len(arr)

    return answer

solution([1,2,3,4])
